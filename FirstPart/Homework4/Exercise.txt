Почитать про enum и добавить минимум 1 шт. в текущий проект(enum должен быть с переменными и методами)
Добавить final class, method, variable (минимум по 1 шт., у кого нет)
Добавить static block, method, variable (минимум по 1 шт., у кого нет)
Добавить конструктор с параметрами (минимум по 1 шт., у кого нет)
Создать репозиторий на github у кого ещё нет
У кого это всё есть и тем кто быстро справится, нужно почистить свою программу, а именно удалить или задействовать не используемые в логике классы, методы, переменные (кроме тех, которые требуются в этом задании, удалить только то, что касается предыдущих дз), разбить большие методы на маленькие, если если есть повторяющийся код вынести в private методы и вызвать их несколько раз. Если есть хардкод заменить его на генерацию рандомных значений. Генерацию всех объектов, которым нужны данные, но мы их не вводим из консоли можно вынести в отдельные public классы (пример EmployeeFactory или EmployeeBuilder с методами типа generateEmployee(), которые возвращают объект).